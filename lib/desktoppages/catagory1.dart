import 'package:flutter/material.dart';

class Catagoryscroller extends StatelessWidget {
  const Catagoryscroller({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
